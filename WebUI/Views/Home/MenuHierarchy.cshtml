@using WebUI.Helpers;
@model String

@foreach (var item in @Model.Split(';'))
{
    string[] link = item.Split(':');
    @Html.ActionLink("-> " + @Html.GetActionMenuTitle((string)link[1]).ToString(), (string)link[1], link[0])
}
